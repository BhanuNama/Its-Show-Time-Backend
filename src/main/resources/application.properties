# ===============================
# DATABASE CONFIG
# ===============================
spring.datasource.url=${SHOWTIME_DB_URL:jdbc:mysql://localhost:3306/showtime}
spring.datasource.username=${SHOWTIME_DB_USERNAME:root}
spring.datasource.password=${SHOWTIME_DB_PASSWORD:Bhanu@0803}

# ===============================
# JPA / HIBERNATE CONFIG
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# SERVER
# ===============================
server.port=8080
